/.row
/  .span6
/    %h4 Случайные объявления
/    %h5=link_to 'Все объявления', ads_path
/    - @ads.each do |ad|
/      .col-md-4
/        - if ad.images[0]
/          = image_tag ad.images[0].image.small
/        - else
/          Фото отсутствует
/      .col-md-8
/        - if can? :read, Ad
/          = link_to "#{Automark.find(ad.automark_id).name} #{Automodel.find(ad.automodel_id).name}, #{ad.city}, #{ad.mileage} км", ad_path(ad)
/        - else
/          #{Automark.find(ad.automark_id).name} #{Automodel.find(ad.automodel_id).name}, #{ad.city}, #{ad.mileage} км
/      %br
/  %h4 Продать автомобиль
/  .span6
/    - unless current_user
/      = render 'devise/registrations/form'
/    - else
/      = render 'ads/form'

.container
  .jumbotron.hero-spacer
    %h1 A Warm Welcome!
    %p Introduce your website content using this jumbotron! It's an awesome way to call attention to something that needs to be read! It's also a great alternative to using a banner image if you don't have a good quality picture!
    %p
      %a.btn.btn-primary.btn-large Call to action!
  %hr/
  .row
    .col-lg-12
      %h3 Случайные объявления
  / /.row
  .row.text-center
    - @ads.each do |ad|
      .col-lg-3.col-md-6.hero-feature
        .thumbnail
          - if ad.images[0]
            = image_tag ad.images[0].image.big
          - else
            .the-icons
              %span.glyphicon.glyphicon-camera
          .caption
            %h4= link_to "#{ad.automark.try(:name)} #{ad.automodel.try(:name)}", ad_path(ad)

  / /.row
  %hr/
  %footer
    .row
      .col-lg-12
        %p Copyright © SomeCar 2013
/ /.container